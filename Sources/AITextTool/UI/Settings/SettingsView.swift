// SettingsView.swift
// AITextTool
//
// Tab container for settings (General, Providers, Prompts, Logs).

import SwiftUI

struct SettingsView: View {
    var body: some View {
        Text("Settings")
    }
}
