// ProvidersSettingsView.swift
// AITextTool
//
// API keys, OAuth status, model picker per provider.

import SwiftUI

struct ProvidersSettingsView: View {
    var body: some View {
        Text("Providers Settings")
    }
}
