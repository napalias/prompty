// ActionBarView.swift
// AITextTool
//
// Keyboard hint bar shown at bottom of streaming and chat views.

import SwiftUI

struct ActionBarView: View {
    var body: some View {
        Text("Action Bar")
    }
}
